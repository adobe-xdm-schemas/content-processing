{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "required": [
    "elementsToExtract"
  ],
  "properties": {
    "elementsToExtract": {
      "type": "array",
      "items": {
        "enum": [
          "text",
          "tables"
        ],
        "type": "string"
      },
      "description": "List specifying elements whose structured information need to be extracted from the PDF document. List must have values from  Text,Table."
    },
    "renditionsToExtract": {
      "type": "array",
      "items": {
        "enum": [
          "figures",
          "tables"
        ],
        "type": "string"
      },
      "description": "List specifying elements whose renditions are required."
    }
  }
}